# Python 3.10+ required (tested with 3.10, 3.11, 3.12)
# conda create -n ttv python=3.10

# Build dependencies (fixes pkg_resources error on new pip)
setuptools>=69.0.0
wheel>=0.42.0

# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Vision models — SAM 3 / SAM 2
# Install from source:
# pip install git+https://github.com/facebookresearch/sam2.git
# pip install git+https://github.com/facebookresearch/sam3.git

# Detection — DINO-X (SOTA) / Grounding DINO (fallback)
# pip install git+https://github.com/IDEA-Research/dino-x-api.git
# Or use HuggingFace (auto-downloads): transformers>=4.47.0

# UI Detection — OmniParser v2
# Clone from: https://github.com/microsoft/OmniParser

# OCR
paddlepaddle>=2.6.0
paddleocr>=2.9.0

# Depth — Depth Pro
# Clone from: https://github.com/apple/ml-depth-pro
# Or use HuggingFace transformers:
transformers>=4.47.0

# 3D Reconstruction — VGGT (Meta, CVPR 2025) / MASt3R (fallback)
# VGGT via HuggingFace: transformers>=4.47.0 (auto-downloads meta/VGGT)
# MASt3R fallback: clone from https://github.com/naver/mast3r
einops>=0.8.0
timm>=1.0.0

# 3D Gaussian Splatting
# pip install gsplat
# pip install nerfstudio
plyfile>=1.0

# Tracking — BoT-SORT (upgraded from StrongSORT)
# pip install ultralytics  (includes BoT-SORT tracker)
# Or standalone: pip install botsort

# Video processing
opencv-python>=4.10.0
ffmpeg-python>=0.2.0
Pillow>=11.0.0

# Math
numpy>=1.26.0
scipy>=1.14.0
scikit-image>=0.24.0
scikit-learn>=1.6.0

# PSD export
# pip install PhotoshopAPI  (open source)
# Or: pip install aspose-psd  (commercial)

# Testing
pytest>=8.3.0
httpx>=0.28.0

# Optional — 3D mesh reconstruction (Poisson surface reconstruction)
# open3d>=0.18.0

# Optional — UV unwrapping (higher quality than built-in sphere/box projection)
# pip install xatlas
